import { describe, expect, it } from 'vitest';

describe('doctor workspace', () => {
  it('has the expected application identity', () => {
    expect('@carebridge/doctor').toBe('@carebridge/doctor');
  });
});
