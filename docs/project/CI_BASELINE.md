# CI baseline

The repository CI baseline uses pnpm 10.15.0, Node.js 22, a pinned TypeScript 5.9.x toolchain for the current lint stack, and does not require a pnpm lockfile for pull-request validation yet.

Local, test and QA environments remain synthetic-data only.
