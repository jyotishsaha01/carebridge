# Builds 04–06

## 04 Admin Portal
Prototype dashboard added at `apps/admin/app/dashboard/page.tsx` with operational KPIs, action queue, filtering and navigation entry points.

## 05 Patient Profile
Prototype profile/settings workspace added at `apps/patient/app/profile/page.tsx` covering personal, emergency, medical, privacy and notification settings.

## 06 Document Storage
A database-independent local object-storage prototype was added at `services/api/src/documentStoragePrototype.ts`. It provides isolated patient storage keys, safe path handling, size enforcement, atomic create, read and delete operations. Replace with a production object-storage adapter before launch.

These prototypes intentionally avoid changing the existing Prisma schema. Production persistence and provider credentials remain a later integration step.
